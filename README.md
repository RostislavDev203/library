# Криптобиржа API

API для криптобиржи с поддержкой аутентификации пользователей, получения цен криптовалют и покупки криптовалют.

## Возможности

- 🔐 Регистрация и аутентификация пользователей
- 💰 Получение актуальных цен криптовалют (BTC, ETH, USDT, XRP, BNB)
- 📊 WebSocket для получения обновлений цен в реальном времени
- 🛒 Покупка криптовалют
- 🗄️ Хранение данных пользователей в SQLite базе данных

## Технологии

- **FastAPI** - веб-фреймворк
- **SQLAlchemy** - ORM для работы с базой данных
- **aiosqlite** - асинхронный драйвер для SQLite
- **PyJWT** - работа с JWT токенами
- **bcrypt** - хеширование паролей
- **httpx** - асинхронные HTTP запросы к API CoinGecko

## Установка и запуск

### Вариант 1: Использование Docker (рекомендуется)

1. Соберите Docker образ:
```bash
docker-compose build
```

2. Запустите контейнер:
```bash
docker-compose up
```

3. API будет доступно по адресу: http://localhost:8000
4. Документация API: http://localhost:8000/docs

### Вариант 2: Локальная установка

1. Установите зависимости:
```bash
pip install -r requirements.txt
```

2. Запустите приложение:
```bash
uvicorn src.main:app --reload
```

## API Endpoints

### Аутентификация

- `POST /auth/sign-in` - Регистрация нового пользователя
- `POST /auth/log-in` - Вход в систему (возвращает JWT токен)

### Главная страница

- `GET /home/main` - Получить текущие цены всех криптовалют

### Покупка криптовалют

- `POST /buy/buycrypto` - Купить криптовалюту (требуется JWT токен)

### WebSocket

- `WS /home/` - WebSocket соединение для получения обновлений цен в реальном времени

## Примеры использования

### Регистрация пользователя

```bash
curl -X POST "http://localhost:8000/auth/sign-in" \
  -H "Content-Type: application/json" \
  -d '{"login": "user123", "password": "password123"}'
```

### Вход в систему

```bash
curl -X POST "http://localhost:8000/auth/log-in" \
  -H "Content-Type: application/json" \
  -d '{"login": "user123", "password": "password123"}'
```

### Получение цен криптовалют

```bash
curl "http://localhost:8000/home/main"
```

### Покупка криптовалюты

```bash
curl -X POST "http://localhost:8000/buy/buycrypto" \
  -H "Content-Type: application/json" \
  -d '{
    "token": "YOUR_JWT_TOKEN",
    "type": "BTC",
    "amount": 0.1
  }'
```

## Структура проекта

```
.
├── src/
│   ├── main.py          # Главный файл приложения
│   ├── database.py      # Модели и функции работы с БД
│   ├── auth.py          # Функции для работы с JWT токенами
│   ├── router_file.py   # API роутеры
│   └── utils.py         # Утилиты (цены криптовалют, аутентификация)
├── requirements.txt     # Зависимости проекта
├── Dockerfile          # Docker образ
├── docker-compose.yml  # Docker Compose конфигурация
└── README.md          # Документация
```

## Безопасность

- Пароли хранятся в захешированном виде (bcrypt)
- JWT токены используются для аутентификации
- Все операции с базой данных выполняются асинхронно

## Примечания

- База данных SQLite создается автоматически при первом запуске
- Цены криптовалют получаются из API CoinGecko
- WebSocket обновляет цены каждые 5 секунд

P.S.
Большая часть кода - я
Комментарии, Docker, README и некая часть кода - CursorAI

